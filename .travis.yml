# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R

r:
  - oldrel
  - release
  - devel

sudo: false

cache: packages

addons:
  apt:
    packages:
      - oracle-java8-set-default
      - graphviz
      - ghostscript

####
